[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "electronics-labs"
version = "0.1.0"
description = "Electronics lab scripts and shared utils"

[tool.setuptools.packages.find]
where = ["."]
include = ["utils*"]
